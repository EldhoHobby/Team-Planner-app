# dependencies
/node_modules

# next.js
/.next
/out

# production
/build

# env & secrets
.env
.env*.local

# prisma
/prisma/*.db

# local uploads (dev only; in prod this is a Docker volume)
/uploads

# database backups (host folder written by the backup sidecar)
/backups/*.dump
/backups/*.dump.part

# misc
.DS_Store
*.pem
npm-debug.log*
*.tsbuildinfo
next-env.d.ts

# local agent / tooling cache (not part of the app)
/.claude

# docker image bundles for offline deploy (large; never commit)
*.tar

# scratch/notes
process.txt
